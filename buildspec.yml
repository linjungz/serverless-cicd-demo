version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8

  build:
    commands:
      - sam build
      - sam package --output-template-file packaged.yaml --s3-bucket $S3BUCKET --s3-prefix $S3PREFIX
      - s3 cp packaged.yaml s3://$S3BUCKET/$S3PREFIX/